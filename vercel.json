{
  "installCommand": "npm install && npm run build",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ],
  "functions": {
    "api/generate/index.js": {
      "maxDuration": 60
    }
  }
}
